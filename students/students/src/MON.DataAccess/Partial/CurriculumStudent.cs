
namespace MON.DataAccess
{
    using System;

    public partial class CurriculumStudent
    {
        //public double? ComputedTotalTermHours
        //{
        //    get
        //    {
        //        double? totalTermHours = ((this.WeeksFirstTerm ?? 0) * (this.HoursWeeklyFirstTerm ?? 0f)) + ((this.WeeksSecondTerm ?? 0) * (this.HoursWeeklySecondTerm ?? 0f));

        //        return totalTermHours > 0 ? Math.Round(totalTermHours.Value, 2, MidpointRounding.AwayFromZero) : (double?)null;
        //    }
        //}
    }
}