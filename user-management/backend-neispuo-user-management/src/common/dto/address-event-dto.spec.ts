import { AddressEventDto } from './telelink/address-event-dto';

describe('AddressEventDto', () => {
    it('should be defined', () => {
        expect(new AddressEventDto()).toBeDefined();
    });
});
