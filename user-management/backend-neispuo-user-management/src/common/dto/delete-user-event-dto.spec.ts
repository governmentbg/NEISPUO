import { DeleteUserEventDto } from './telelink/delete-user-event-dto';

describe('DeleteUserEventDto', () => {
    it('should be defined', () => {
        expect(new DeleteUserEventDto()).toBeDefined();
    });
});
