import { EventResponseDto } from './telelink/event-response-dto';

describe('EventResponseDto', () => {
    it('should be defined', () => {
        expect(new EventResponseDto()).toBeDefined();
    });
});
